<h1>index with map</h1>
<div class="container">
  <div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>

</div>

<h1>Les evenements pour toi :</h1>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-sm-8">
      <div id="events">
        <% if @events.any? %>
          <% @events.each do |event| %>
            <h4><%= event.name %></h4>
            <p><%= event.description %></p>
            <p><%= event.tags %></p>
            <p><%= link_to "En savoir plus", event_path(event), class: "btn btn-warning" %>
          <% end %>
        <% else %>
          <em>Désolé, aucun événement n'a été trouvé.</em>
        <% end %>
      </div>
    </div>
  </div>
</div>
