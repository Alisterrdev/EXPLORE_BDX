<section class="event-show">
  <!-- HERO -->
  <div class="event-hero">
    <div class="event-hero__image">
      <%= image_tag @event.image || "placeholder.jpeg", alt: @event.name %>
    </div>
    <div class="event-hero__content">
      <h1 class="event-title"><%= @event.name %></h1>
      <p class="event-subtitle"><%= @event.tags %></p> <!-- texte statique -->

      <div class="event-card">
        <p><strong>📍 Lieu :</strong> <%= @event.address %></p>
        <p><strong>📅 Date :</strong> <%= I18n.l(@event.date) %></p>
        <p><strong>⏰ Heure :</strong> 20h00</p> <!-- texte statique -->
      </div>
      <%# <p><%= link_to "Ca m'intéresse", favorites_path, class: "btn btn-success" %></p>
      <%# <%= link_to "Ajouter un favori", event_favorites_path(@event), data: { turbo_method: :post } %>
<%= link_to event_favorites_path(@event), method: :post, data: { turbo_method: :post }, class: "favorite-link" do %>
  Ajouter à mes favoris
  <i class="heart-icon fa-regular fa-heart" style="color: red;"></i>
<% end %>

    </div>
  </div>


  <!-- DESCRIPTION / GALERY -->
  <section class="event-description">
    <%#<h2>À propos de l’événement</h2>
    <%#<p><%= @event.description %></p>
   <%#<p><%= @event.details %><p>
<section class="gallery-block">
  <div class="gallery-text">
    <h2>À propos de l’événement</h2>
      <h3>Le grand dévoilement</h3>
        <p><%= @event.description %></p>
      <h3>La fête continue</h3>
        <p><%=@event.details%></p>
  </div>
  <div class="gallery-images">
<% if @event.files.attached? %>
<% @event.files.each_with_index do |file, index| %>
    <div class="image-container image-<%= index + 1 %>">
      <%= image_tag file, alt: "Description 1" %>
    </div>
<% end %>
<% else %>
    <div class="image-container image-1">
      <%= image_tag "mood-pic/festif/demo/demo-1.jpg", alt: "Description 1" %>
    </div>
    <div class="image-container image-2">
      <%= image_tag "mood-pic/festif/demo/demo-2.jpg", alt: "Description 2" %>
    </div>
    <div class="image-container image-3">
      <%= image_tag "mood-pic/festif/demo/demo-3.jpg", alt: "Description 3" %>
    </div>
    <div class="image-container image-4">
      <%= image_tag "mood-pic/festif/demo/demo-4.jpg", alt: "Description 4" %>
    </div>
<% end %>
</div>
</section>


  <!-- AUTRES EVENTS -->
  <section class="event-suggestions">
    <h2>D'autres événements qui pourraient vous intéresser</h2>
    <div class="event-cards">
      <% if @events.any? %>
        <% @events.each do |event| %>
          <div class="event-card">
            <%= image_tag event.image || "placeholder.jpg", alt: event.name, class: "event-card-image" %>
            <h3><%= event.name %></h3>
            <p><%= I18n.l(event.date) %></p>
            <p><%= event.description %></p>
            <a href="<%= event_path(event) %>" class="btn-secondary">En savoir plus</a>
          </div>
        <% end %>
      <% else %>
        <em>Désolé, aucun événement n'a été trouvé.</em>
      <% end %>
    </div>
  </section>
</section>
