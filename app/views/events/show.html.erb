<section class="event-show">

  <!-- HERO -->
  <div class="event-hero">
    <div class="event-hero__image">
      <%= image_tag @event.image || "placeholder.jpeg", alt: @event.name %>

    </div>
    <div class="event-hero__content">
      <h1 class="event-title"><%= @event.name %></h1>
      <p class="event-subtitle"><%= @event.tags %></p> <!-- texte statique -->

      <div class="event-card">
        <p><strong>📍 Lieu :</strong> <%= @event.address %></p>
        <p><strong>📅 Date :</strong> <%= I18n.l(event.date) %></p>
        <p><strong>⏰ Heure :</strong> 20h00</p> <!-- texte statique -->
      </div>

      <%# <p><%= link_to "Ca m'intéresse", favorites_path, class: "btn btn-success" %></p>
      <%# <%= link_to "Ajouter un favori", event_favorites_path(@event), data: { turbo_method: :post } %>
<%= link_to event_favorites_path(@event), method: :post, data: { turbo_method: :post }, class: "favorite-link" do %>
  <i class="heart-icon fa-regular fa-heart" style="color: red;"></i>
<% end %>
    </div>
  </div>

  <!-- DESCRIPTION / GALERY -->
  <section class="event-description">
    <h2>À propos de l’événement</h2>
    <p><%= @event.description %></p>
    <p><%= @event.details %><p>

<section class="gallery-block">
  <div class="gallery-text">
    <h2>Mais encore ?</h2>
      <h3>Un événement international</h3>
        <p>Créé en 2025, le BDX Street Arts est un festival pluridisciplinaire célébrant les arts de rue sous toutes leurs formes – jonglerie, musique, danse, théâtre et arts visuels –, mettant en lumière les performances hybrides et surprenantes. </p>
      <h3>La ligne éditoriale</h3>
        <p>Le festival propose spectacles, concerts, ateliers participatifs, rencontres et déambulations. Oscillant entre culture populaire et initiatives innovantes, sa programmation se veut ludique, festive, décalée et accessible, mêlant émotion et étonnement.</p>
      <h3>Une expérience à vivre</h3>
        <p>Le BDX Street Arts cherche à surprendre le public là où on ne l’attend pas, pour faire vibrer la ville et créer un lien vivant avec ses habitants. Le festival invite chacun à s’immerger dans les créations, à participer aux ateliers et à partager une expérience culturelle unique, hors des sentiers battus.</p>
  </div>

  <div class="gallery-images">
    <div class="image-container image-1">
      <%= image_tag "image1.jpg", alt: "Description 1" %>
    </div>
    <div class="image-container image-2">
      <%= image_tag "image2.jpg", alt: "Description 2" %>
    </div>
    <div class="image-container image-3">
      <%= image_tag "image3.jpg", alt: "Description 3" %>
    </div>
    <div class="image-container image-4">
      <%= image_tag "image4.jpg", alt: "Description 4" %>
    </div>
  </div>

</section>


  <!-- AUTRES EVENTS -->
  <section class="event-suggestions">
    <h2>Les autres événements qui pourraient vous intéresser</h2>
    <div class="event-cards">
      <% if @events.any? %>
        <% @events.each do |event| %>
          <div class="event-card">
            <%= image_tag event.image || "placeholder.jpg", alt: event.name, class: "event-card-image" %>
            <h3><%= event.name %></h3>
            <p><%= I18n.l(event.date) %></p>
            <p><%= event.description %></p>
            <a href="<%= event_path(event) %>" class="btn-secondary">En savoir plus</a>
          </div>
        <% end %>
      <% else %>
        <em>Désolé, aucun événement n'a été trouvé.</em>
      <% end %>
    </div>
  </section>

</section>
